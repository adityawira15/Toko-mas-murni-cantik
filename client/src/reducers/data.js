let initialState = []

export default function data(state = initialState, action){
    switch(action.type){
        case 'text':
        console.log('masuk')
            return state
        default:
         return state
    }
}